# pyczan 项目开发说明书

> **用途**：供 AI Agent 阅读理解项目全貌，并据此完成 C++/Python 混合开发。
> **更新**：v0.1.0 已完成首个模块 pyczan_shmem 的 C++ 核心和 Python 绑定。

---

## 1. 项目概述

`pyczan` 是一个 **C++ 高性能 Python 加速工具集**，旨在为 Python 开发者提供一系列底层高性能模块，解决 Python 在 CPU 密集型和 I/O 密集型场景下的性能瓶颈。

### 1.1 核心原则

- **功能单一**：每个模块只做一件事，做到极致性能。
- **按需安装**：用户只安装自己需要的模块，不捆绑臃肿依赖。
- **易用性**：Python API 与原生 Python 习惯保持一致（如 dict-like 接口）。
- **长期维护**：项目以十年为周期进行规划，代码质量要求高。
- **零多余依赖**：优先使用操作系统原生 API，不引入可有可无的第三方库。

### 1.2 当前阶段

- **项目名**：`pyczan`
- **版本**：v0.1.0（开发中）
- **代码仓库**：Gitee 为主仓库，GitHub 为镜像（通过 Gitee 同步）
- **支持平台**：仅 Windows 10/11（64位）
- **构建工具**：MSVC（Visual Studio 2022 BuildTools）+ MSBuild

### 1.3 版本规划

| 版本   | 里程碑     | 内容                                           |
| :----- | :--------- | :--------------------------------------------- |
| v0.0.0 | ✅ 已完成   | PyPI 空包占位                                  |
| v0.1.0 | 🎯 当前目标 | 第一个 C++ 模块 `pyczan_shmem`（共享内存字典） |
| v0.2.0 | 未来       | 更多性能模块（fastio、simd 等）                |

---

## 2. 技术栈总览

| 层级            | 技术选型                          | 说明                         |
| :-------------- | :-------------------------------- | :--------------------------- |
| **C++ 标准**    | C++17                             | MSVC 完全支持                |
| **编译器**      | MSVC（VS2022 BuildTools）         | 仅 Windows 10/11 64位        |
| **构建系统**    | MSBuild（.sln/.vcxproj）          | 原生 VS2022 项目文件         |
| **Python 绑定** | nanobind 1.2.0（预编译静态库）    | 轻量级绑定                   |
| **共享内存**    | Win32 API（CreateFileMapping）    | 零第三方依赖                 |
| **进程同步**    | Win32 API（CreateMutex）          | 零第三方依赖                 |
| **Python 打包** | setuptools + build                | 使用 pyproject.toml          |

---

## 3. 目录结构

```
pyczan/
├── src/
│   ├── cpp/                          ← C++ 独立项目
│   │   ├── include/pyczan_shmem/     ← 公共 C++ 接口头文件
│   │   │   └── shmem_dict.hpp        ← SharedMemoryDict 类声明
│   │   ├── pyczan_shmem/             ← 模块源码
│   │   │   ├── shmem_dict.cpp        ← 核心实现（编入静态库）
│   │   │   ├── bindings.cpp          ← nanobind Python 绑定
│   │   │   └── test_shmem.cpp        ← 独立 C++ 测试程序
│   │   ├── vc17/                     ← VS2022 项目文件
│   │   │   ├── pyczan_shmem.sln      ← 解决方案
│   │   │   ├── pyczan_core/          ← 静态库项目 → pyczan_shmem.lib
│   │   │   ├── _pyczan_shmem/        ← .pyd 项目 → _pyczan_shmem.pyd
│   │   │   └── test_shmem/           ← 测试项目 → test_shmem.exe
│   │   ├── lib/                      ← 预编译依赖 + 构建产物
│   │   │   ├── nanobind/             ← 预编译 nanobind（头文件 + .lib）
│   │   │   ├── pyczan_shmem.lib      ← Release 静态库
│   │   │   └── pyczan_shmemd.lib     ← Debug 静态库（带 d 后缀）
│   │   ├── build_debug.bat           ← Debug 构建脚本
│   │   └── build_release.bat         ← Release 构建脚本
│   │
│   └── py/                           ← Python 包
│       ├── pyczan/                   ← 命名空间包
│       │   └── __init__.py
│       └── pyczan_shmem/             ← C++ 扩展的 Python 封装
│           ├── __init__.py           ← from ._pyczan_shmem import SharedMemoryDict
│           └── _pyczan_shmem.pyd     ← 编译产物
│
├── tests/
│   ├── __init__.py
│   └── test_shmem.py                 ← pytest 测试
│
├── pyproject.toml                    ← Python 包配置
├── CLAUDE.md                         ← AI 辅助开发文档
├── 项目开发说明书.md                  ← 本文档
├── C++编码规范.md                     ← C++ 编码规范
├── 上传到pypi步骤.txt                 ← PyPI 发布步骤
└── .gitignore
```

---

## 4. 构建系统（MSBuild + VS2022）

### 4.1 解决方案结构

解决方案 `src/cpp/vc17/pyczan_shmem.sln` 包含三个项目：

1. **pyczan_core**（静态库）—— 编译 `shmem_dict.cpp`，输出 `pyczan_shmem.lib` / `pyczan_shmemd.lib`
2. **_pyczan_shmem**（动态库 .pyd）—— 编译 `bindings.cpp`，链接 `pyczan_core` + `nanobind`
3. **test_shmem**（可执行文件）—— 编译 `test_shmem.cpp`，链接 `pyczan_core`

### 4.2 构建命令

```bash
# Release 构建
src\cpp\build_release.bat

# Debug 构建
src\cpp\build_debug.bat
```

### 4.3 输出产物

| 项目 | Release | Debug |
|------|---------|-------|
| pyczan_core | `lib/pyczan_shmem.lib` | `lib/pyczan_shmemd.lib` |
| _pyczan_shmem | `py/pyczan_shmem/_pyczan_shmem.pyd` | 同上 |
| test_shmem | `build/bin/Release/test_shmem.exe` | `build/bin/Debug/test_shmem.exe` |

### 4.4 CRT 运行时说明

- `_pyczan_shmem.pyd` 始终使用 `/MD`（Release CRT），以匹配 Python 和 nanobind 的运行时
- `pyczan_core` 和 `test_shmem` 使用 `/MDd`（Debug）/ `/MD`（Release），各自独立
- Debug 模式下 `.pyd` 链接 Release 版静态库（`pyczan_shmem.lib`），其他项目链接 Debug 版（`pyczan_shmemd.lib`）

---

## 5. 共享内存设计（Win32 API）

### 5.1 核心 API

| Windows API | 用途 |
|---|---|
| `CreateFileMappingA` | 创建/打开命名共享内存段 |
| `MapViewOfFile` | 映射共享内存到进程地址空间 |
| `UnmapViewOfFile` | 取消映射 |
| `CloseHandle` | 关闭句柄 |
| `CreateMutexA` | 创建/打开命名互斥体 |
| `WaitForSingleObject` | 加锁 |
| `ReleaseMutex` | 解锁 |

### 5.2 内存布局

```
+------------------------------------------+
| SHMEM_HEADER (16 字节)                    |
|   magic    : uint32_t  (0x53484D44)      |
|   capacity : uint32_t  (总容量)           |
|   dataSize : uint32_t  (当前数据大小)      |
|   count    : uint32_t  (条目数)           |
+------------------------------------------+
| DATA AREA (序列化的 key-value 列表)        |
|   keyLen(4B) | keyData | valLen(4B) | valData |
|   keyLen(4B) | keyData | valLen(4B) | valData |
|   ...                                     |
+------------------------------------------+
```

### 5.3 序列化格式

每个 Entry 的二进制格式：
```
keyLen(uint32_t) | keyData(char[keyLen]) | valueLen(uint32_t) | valueData(char[valueLen])
```

### 5.4 命名约定

| 对象 | 命名规则 | 示例 |
|---|---|---|
| 共享内存 | `pyczan_shmem_{name}` | `pyczan_shmem_test` |
| 互斥体 | `pyczan_shmem_mtx_{name}` | `pyczan_shmem_mtx_test` |

### 5.5 线程/进程安全

所有读写操作通过 `WaitForSingleObject` / `ReleaseMutex` 加锁，保证多进程并发安全。

---

## 6. 核心 C++ 类设计

### 6.1 SharedMemoryDict 类

**文件**：`src/cpp/include/pyczan_shmem/shmem_dict.hpp`

```cpp
namespace pyczan_shmem {

class SharedMemoryDict
{
public:
    SharedMemoryDict();
    ~SharedMemoryDict();

    bool OpenOrCreate(const std::string& name, std::size_t size = 65536);
    void Close();

    void Set(const std::string& key, const std::string& value);
    std::string Get(const std::string& key);
    bool Delete(const std::string& key);
    bool Has(const std::string& key);
    std::size_t Size();
    void Clear();
    std::vector<std::string> Keys();

    bool IsOpen() const;
    const std::string& Name() const;

private:
    HANDLE  m_hFileMapping;   // CreateFileMapping 句柄
    LPVOID  m_pView;          // MapViewOfFile 指针
    HANDLE  m_hMutex;         // CreateMutex 句柄
    std::string m_name;
    std::size_t m_size;
};
```

### 6.2 Python 绑定（nanobind）

**文件**：`src/cpp/pyczan_shmem/bindings.cpp`

导出的 Python API：

| Python 用法 | C++ 方法 | 说明 |
|---|---|---|
| `d = SharedMemoryDict()` | 构造函数 | 初始未打开状态 |
| `d.OpenOrCreate(name)` | `OpenOrCreate()` | 打开或创建 |
| `d.Close()` | `Close()` | 关闭 |
| `d["key"] = "val"` | `Set()` | 设置键值对 |
| `d["key"]` | `Get()` | 获取值（KeyError 可抛出） |
| `del d["key"]` | `Delete()` | 删除（KeyError 可抛出） |
| `"key" in d` | `Has()` | 检查键存在 |
| `len(d)` | `Size()` | 获取条目数 |
| `d.Keys()` | `Keys()` | 获取所有键 |
| `d.Clear()` | `Clear()` | 清空 |
| `d.IsOpen()` | `IsOpen()` | 是否已打开 |
| `d.Name()` | `Name()` | 获取共享内存名 |
| `with d:` | 上下文管理器 | 自动 Close |

### 6.3 C++ 测试程序

**文件**：`src/cpp/pyczan_shmem/test_shmem.cpp`

独立 C++ 测试程序，使用自定义 TEST/CHECK/PASS 宏，不依赖任何测试框架。测试内容包括：
- 打开/关闭生命周期
- Set/Get 基本操作
- 覆盖已有值
- Has/Delete/Size/Clear
- Keys 枚举
- 未打开时返回空

---

## 7. 编码规范要点

AI Agent 在编写所有 C++ 代码时**必须严格遵守**《C++编码规范.md》的强制规则：

- 头文件使用 `#ifndef/#define/#endif` 保护（宏名如 `SHMEM_DICT_H_`）。
- 类名/函数名 `PascalCase`（`SharedMemoryDict`），变量/参数 `camelCase`（`m_pView`）。
- 成员变量 `m_` 前缀，静态变量 `s_` 前缀，全局变量 `g_` 前缀。
- BSD/Allman 大括号风格（独占一行），缩进 4 空格。
- 行宽 80 字符以内。
- 指针/引用修饰符紧靠变量名（`char* name`）。
- 函数入口使用 `assert` 检查参数有效性。
- 动态内存申请后检查空指针，释放后置 `nullptr`。
- 避免复杂复合表达式，使用括号明确优先级。

---

## 8. nanobind 说明

- nanobind 已**预编译**为静态库，头文件和 `.lib` 放在 `src/cpp/lib/nanobind/` 下
- 编译方式（参考，已执行）：
  ```bash
  git clone --depth 1 --branch v1.2.0 https://github.com/wjakob/nanobind.git
  cd nanobind && git submodule update --init
  cmake -B build -G "Visual Studio 17 2022" -DNB_TEST=OFF
  cmake --build build --config Release --target nanobind-static
  ```
- 只需 Release 版即可，Debug 构建时 `.pyd` 项目也使用 Release CRT（`/MD`）

---

## 9. 测试

### 9.1 C++ 测试

```bash
src\cpp\build\bin\Release\test_shmem.exe
```

### 9.2 Python 测试

```bash
pytest tests/test_shmem.py -v
```

---

## 10. 版本历史

| 版本 | 日期       | 变更                                              |
| :--- | :--------- | :------------------------------------------------ |
| v3.0 | 2026-07-25 | 全面重构：C++/Python 分离、Win32 API 替代 Boost、MSBuild 替代 CMake |
| v2.0 | 2026-07-25 | 增加 VS2022 开发配置、独立 C++ 测试、编码规范说明 |

---

**文档结束**。AI Agent 请严格按照上述规格编写代码，确保在 VS2022 中顺利编译运行。
