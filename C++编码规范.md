# C++ 编程规范（AI Agent 严格遵从版）

本规范基于提供的 JSON 规则与建议整理而成，分为**强制性规则（Rules）**和**推荐性建议（Suggestions）**。AI Agent 在生成 C++ 代码时，**必须遵守所有强制性规则**，并尽量采纳推荐性建议，以确保代码质量、可读性、健壮性和可维护性。

---

## 1. 头文件与预处理

### 1.1 头文件重复引用保护 【R1-2-1】 *强制*
- 每个头文件必须使用 `#ifndef/#define/#endif` 结构防止重复包含。
- 命名约定：宏名应基于文件名（全大写，下划线分隔），如 `#ifndef MY_MODULE_H_`。

### 1.2 头文件引用方式 【R1-2-2】【R1-2-3】 *强制*
- 标准库头文件使用 `#include <filename.h>`（编译器从标准库目录搜索）。
- 非标准库（项目内）头文件使用 `#include "filename.h"`（编译器从用户工作目录搜索）。

### 1.3 模板类定义例外 【R1-2-4】 *强制*
- 模板类（template class）的定义可以放在头文件中，这是 C++ 语言的必要要求，视为合理例外。

### 1.4 头文件内容建议 【S1-2-1】【S1-2-2】 *建议*
- 头文件只存放“声明”，不存放“定义”（类的成员函数定义与声明分开，即使函数体很小）。
- 不提倡使用全局变量，尽量避免在头文件中出现 `extern int value` 这类声明。

---

## 2. 代码格式与布局

### 2.1 空行使用 【R2-1-1】【R2-1-2】 *强制*
- 每个类声明之后、每个函数定义结束之后都要加空行。
- 函数体内，逻辑上密切相关的语句之间不加空行，其他地方（如不同逻辑块之间）应加空行分隔。

### 2.2 每行一条语句 【R2-2-1】 *强制*
- 一行代码只做一件事情：只定义一个变量，或只写一条语句。
- 不允许在一行内写多个语句或定义多个变量。

### 2.3 控制结构独占一行及括号 【R2-2-2】 *强制*
- `if`、`for`、`while`、`do` 等语句自占一行，执行语句不得紧跟其后。
- 不论执行语句多少，都必须使用 `{}` 括起代码块（即使只有一条语句）。

### 2.4 空格规则
#### 关键字与函数名 【R2-3-1】【R2-3-2】 *强制*
- 关键字（`if`、`for`、`while`、`switch` 等）之后必须留至少一个空格。
- 函数名之后**不要**留空格，紧接左括号 `(`，以区别于关键字。

#### 括号、逗号、分号 【R2-3-3】【R2-3-4】 *强制*
- `(` 向后紧跟（即其前有空格，其后无空格）；`)`、`,`、`;` 向前紧跟（即其前无空格）。
- `,` 之后必须留空格（如 `Function(x, y, z)`）。
- 如果 `;` 不是一行的结束符号（如 `for` 中的分号），其后也要留空格。

#### 操作符空格 【R2-3-5】【R2-3-6】【R2-3-7】 *强制*
- 二元操作符（赋值、比较、算术、逻辑、位域等）前后均应加空格。
- 一元操作符（`!`、`~`、`++`、`--`、取地址 `&`、解引用 `*` 等）前后不加空格。
- `[]`、`.`、`->` 等操作符前后不加空格。

#### 紧凑性建议 【S2-3-1】 *建议*
- 对于表达式较长的 `for` 和 `if` 语句，为紧凑可适当去掉一些空格（如 `for (i=0; i<10; i++)`），但需保持清晰。

### 2.5 大括号与缩进 【R2-4-1】【R2-4-2】 *强制*
- 程序的分界符 `{` 和 `}` 应独占一行，位于同一列，并与引用它们的语句左对齐。
- `{ }` 之内的代码块必须在 `{` 右边数格处左对齐（缩进对齐），缩进通常为 2 或 4 个空格（保持一致）。

### 2.6 行长度与折行 【R2-5-1】【R2-5-2】 *强制*
- 代码行最大长度宜控制在 70～80 个字符以内。
- 长表达式应在低优先级操作符处拆分为新行，操作符放在新行之首，新行适当缩进（通常为上一行起始列 + 缩进量）。

### 2.7 指针/引用修饰符位置 【R2-6-1】 *强制*
- 修饰符 `*` 和 `&` 应紧靠变量名，如 `char *name;`，而非 `char* name;`。

### 2.8 注释规范
#### 注释原则 【R2-7-1】【R2-7-2】 *强制*
- 注释是对代码的“提示”，而非文档，不可喧宾夺主，花样要少。
- 如果代码本身清晰易懂，则不加注释，避免多余注释。

#### 注释维护 【R2-7-3】 *强制*
- 边写代码边注释，修改代码同时修改相应注释，保证注释与代码一致，删除不再有用的注释。

#### 注释质量 【R2-7-4】【R2-7-5】 *强制*
- 注释应准确、易懂，防止二义性。
- 尽量避免使用缩写，特别是不常用缩写。

#### 注释位置 【R2-7-6】 *强制*
- 注释应与被描述的代码相邻，可放在代码上方或右方，不可放在下方。

#### 嵌套结束注释 【R2-7-8】 *强制*
- 当代码较长（尤其多重嵌套）时，应在某些段落结束处加注释（如 `// end of while`），以便识别层次。

---

## 3. 命名规则

### 3.1 通用命名原则
- **直观可拼读** 【R3-1-1】 *强制*：标识符应直观且可拼读，望文知意，不必“解码”。
- **长度原则** 【R3-1-2】 *强制*：符合“min-length && max-information”（简短但信息量最大）。
- **风格一致** 【R3-1-3】 *强制*：命名规则尽量与操作系统/开发工具风格保持一致（如 Windows 用大小写混排，Unix 用小写加下划线；本项目统一采用后文规定）。
- **避免相似标识符** 【R3-1-4】 *强制*：不要出现仅靠大小写区分的相似标识符（如 `int x, X;`）。
- **避免同名作用域冲突** 【R3-1-5】 *强制*：不要出现标识符完全相同的局部变量和全局变量（即使作用域不同）。
- **变量命名语义** 【R3-1-6】 *强制*：变量名使用“名词”或“形容词＋名词”（如 `value`, `oldValue`）。
- **函数命名语义** 【R3-1-7】 *强制*：
  - 全局函数：使用“动词”或“动词＋名词”（动宾词组）。
  - 类的成员函数：只使用“动词”，省略的名词就是对象本身（如 `Get()` 表示获取对象自身属性）。
- **反义词组** 【R3-1-8】 *强制*：互斥意义的变量或相反动作的函数使用正确的反义词组（如 `minValue/maxValue`, `SetValue/GetValue`）。
- **避免数字编号** 【S3-1-1】 *建议*：尽量避免名字中出现数字编号（如 `Value1`, `Value2`），除非逻辑上确实需要。

### 3.2 具体命名风格
- **类名和函数名** 【R3-2-1】 *强制*：使用大写字母开头的单词组合（Pascal 风格），如 `MyClass`, `ComputeTotal`。
- **变量和参数** 【R3-2-2】 *强制*：使用小写字母开头的单词组合（camel 风格），如 `myVariable`, `inputParam`。
- **常量** 【R3-2-3】 *强制*：全部大写，用下划线分割单词，如 `MAX_LENGTH`。
- **静态变量** 【R3-2-4】 *强制*：加前缀 `s_`（如 `s_instance`）。
- **全局变量** 【R3-2-5】 *强制*：加前缀 `g_`（如 `g_config`）。
- **类的数据成员** 【R3-2-6】 *强制*：加前缀 `m_`（如 `m_value`），避免与成员函数参数同名。
- **库前缀** 【R3-2-7】 *强制*（如适用）：为防止标识符冲突，可为标识符加上反映软件性质的前缀（如 OpenGL 的 `gl` 前缀）。

---

## 4. 表达式与语句

### 4.1 运算符优先级 【R4-1-1】 *强制*
- 如果代码行中的运算符较多，使用括号明确操作顺序，避免依赖默认优先级。

### 4.2 复合表达式 【R4-2-1】【R4-2-2】【R4-2-3】 *强制*
- 不要编写太复杂的复合表达式（如 `i = a >= b && c < d && c + f <= g + h;`）。
- 不要使用多用途的复合表达式（如 `d = (a = b + c) + r;` 应拆分为两句）。
- 不要把复合表达式与“真正的数学表达式”混淆（如 `if (a < b < c)` 在 C++ 中不是数学含义）。

### 4.3 变量比较规则
#### 布尔变量 【R4-3-1】 *强制*
- 不可将布尔变量直接与 `TRUE`、`FALSE` 或 `1`、`0` 比较，应直接使用 `if (flag)` 或 `if (!flag)`。

#### 整型变量 【R4-3-2】 *强制*
- 整型变量用 `==` 或 `!=` 直接与 0 比较（如 `if (value == 0)`）。

#### 浮点变量 【R4-3-3】 *强制*
- 不可将浮点变量用 `==` 或 `!=` 与任何数字比较，应转化为 `>=` 或 `<=` 形式，并考虑精度误差（如 `if ((x >= -EPSILON) && (x <= EPSILON))`）。

#### 指针变量 【R4-3-4】 *强制*
- 指针变量用 `==` 或 `!=` 与 `NULL` 比较（如 `if (p == NULL)`），不要写成 `if (p == 0)` 或 `if (p)`。

### 4.4 循环优化建议（效率相关）【S4-4-1】【S4-4-2】 *建议*
- 在多重循环中，将最长的循环放在最内层，最短的放在最外层，以减少 CPU 跨切循环层的次数。
- 如果循环体内存在逻辑判断且循环次数很大，宜将逻辑判断移到循环体外，以提高效率。

### 4.5 for 循环控制 【R4-5-1】 *强制*
- 不可在 for 循环体内修改循环变量，防止循环失控。
- **建议**：循环控制变量的取值采用“半开半闭区间”写法（如 `for (int x = 0; x < N; ++x)`），比闭区间更直观。【S4-5-1】

### 4.6 switch 语句 【R4-6-1】【R4-6-2】 *强制*
- 每个 `case` 语句结尾必须加 `break`，除非有意使多个分支重叠（需明确注释说明）。
- 必须包含 `default` 分支，即使不处理任何情况也写成 `default: break;`，以防别人误以为遗漏处理。

---

## 5. 常量

### 5.1 使用常量代替魔数 【R5-1-1】 *强制*
- 尽量使用含义直观的常量来表示多次出现的数字或字符串（如 `const int MAX = 100;`）。

### 5.2 常量存放位置 【R5-3-1】 *强制*
- 对外公开的常量放在头文件中；不对外公开的常量放在定义文件的头部。

### 5.3 常量间关系 【R5-3-2】 *强制*
- 如果某常量与其他常量密切相关，应在定义中包含这种关系（如 `const double DIAMETER = RADIUS * 2;`）。

---

## 6. 函数设计

### 6.1 参数声明 【R6-1-1】 *强制*
- 参数书写要完整：必须写出参数类型和参数名；若无参数则用 `void` 填充。

### 6.2 参数顺序与命名 【R6-1-2】 *强制*
- 参数命名要恰当，顺序合理（一般目的参数放在前面，源参数放在后面）。

### 6.3 输入指针参数加 const 【R6-1-3】 *强制*
- 如果参数是指针且仅作输入用，应在类型前加 `const`，防止函数体内意外修改指针指向的内容。

### 6.4 对象参数传递效率 【R6-1-4】 *强制*
- 如果输入参数以值传递的方式传递对象，宜改用 `const &` 传递，以提高效率（省去临时对象的构造和析构）。

### 6.5 返回值类型 【R6-2-1】 *强制*
- 不要省略返回值类型；若无返回值，必须声明为 `void`。

### 6.6 函数名与返回值类型语义一致 【R6-2-2】 *强制*
- 函数名字与返回值类型在语义上不可冲突（如 `getchar` 返回 `int` 容易误导，应避免）。

### 6.7 正常值与错误标志分离 【R6-2-3】 *强制*
- 不要将正常值和错误标志混在一起返回；正常值通过输出参数获得，错误标志通过 `return` 语句返回。

### 6.8 入口参数检查 【R6-3-1】 *强制*
- 在函数体入口处对参数有效性进行检查（使用 `assert` 或条件判断）。

### 6.9 出口 return 检查 【R6-3-2】 *强制*
- 在函数体出口处检查 `return` 语句的正确性和效率（如不要返回指向栈内存的指针或引用）。

### 6.10 断言使用 【R6-5-1】【R6-5-2】 *强制*
- 使用断言（`assert`）捕捉不应该发生的非法情况，不要混淆非法情况与错误情况（错误是必然存在的，需处理）。
- 在函数入口处，使用断言检查参数的有效性（合法性）。

### 6.11 参数个数限制 【S6-1-1】 *建议*
- 避免函数参数过多，尽量控制在 5 个以内。

### 6.12 避免变参函数 【S6-1-2】 *建议*
- 尽量不要使用类型和数目不确定的参数（如 `printf` 风格），因为会丧失类型安全检查。

### 6.13 返回值扩展（链式）【S6-2-1】 *建议*
- 有时函数原本不需要返回值，但为支持链式表达式可附加返回值（如 `strcpy` 返回目的地址）。

### 6.14 返回对象效率 【S6-2-2】 *建议*
- 如果函数返回对象，某些场合用“引用传递”替代“值传递”可提高效率，但返回局部对象时不能返回引用。

### 6.15 函数单一职责 【S6-4-1】 *建议*
- 函数功能要单一，不要设计多用途函数。

### 6.16 函数体规模 【S6-4-2】 *建议*
- 函数体尽量控制在 50 行代码以内。

### 6.17 避免“记忆”功能 【S6-4-3】 *建议*
- 尽量避免函数带有“记忆”功能（如 `static` 局部变量），相同输入应产生相同输出。

### 6.18 检查其他输入来源 【S6-4-4】 *建议*
- 不仅要检查输入参数，还要检查通过其他途径进入函数体的变量（如全局变量、文件句柄）的有效性。

### 6.19 错误返回值明确 【S6-4-5】 *建议*
- 用于出错处理的返回值必须清晰，让使用者不易忽视或误解错误情况。

### 6.20 断言设计思想 【S6-5-1】【S6-5-2】 *建议*
- 编写函数时反复考查“我打算做哪些假定？”，并用断言检查假定。
- 防错设计可能隐瞒错误，若“不可能发生”的事情发生，需使用断言报警。

---

## 7. 内存管理

### 7.1 申请内存后检查指针 【R7-2-1】 *强制*
- 使用 `malloc` 或 `new` 申请内存后，立即检查指针是否为 `NULL`，防止使用空指针。

### 7.2 初始化内存 【R7-2-2】 *强制*
- 不要忘记为数组和动态内存赋初值，防止将未初始化的内存作为右值使用。

### 7.3 防止下标越界 【R7-2-3】 *强制*
- 避免数组或指针下标越界，特别注意“多 1”或“少 1”操作。

### 7.4 配对释放 【R7-2-4】 *强制*
- 动态内存的申请与释放必须配对（`malloc/free`，`new/delete` 数量一致），防止内存泄漏。

### 7.5 释放后置空 【R7-2-5】 *强制*
- 使用 `free` 或 `delete` 释放内存后，立即将指针设置为 `NULL`，防止产生“野指针”。

---

## 8. 缺省参数（默认参数）

### 8.1 声明与定义分离 【R8-3-1】 *强制*
- 参数缺省值只能出现在函数的**声明**中，不能出现在定义体中。

### 8.2 缺省顺序 【R8-3-2】 *强制*
- 如果函数有多个参数，缺省参数只能从后向前挨个儿设置，不能跳跃。

---

## 9. 继承与组合（面向对象设计）

### 9.1 无关类不继承 【R10-1-1】 *强制*
- 如果类 A 和 B 毫不相关，不可为了使 B 功能更多而让 B 继承 A。

### 9.2 逻辑上“是一种”才继承 【R10-1-2】 *强制*
- 若在逻辑上 B 是 A 的“一种”（a kind of），且 A 的所有功能和属性对 B 都有意义，才允许 B 继承 A。

### 9.3 “一部分”用组合 【R10-2-1】 *强制*
- 若在逻辑上 A 是 B 的“一部分”（a part of），则不允许 B 从 A 派生，而是用 A 和其它东西组合出 B。

---

## 10. 效率与优化

### 10.1 质量优先于效率 【R11-2-1】 *强制*
- 不要一味追求效率，应在满足正确性、可靠性、健壮性、可读性等前提下设法提高效率。

### 10.2 全局效率优先 【R11-2-2】 *强制*
- 以提高程序的全局效率为主，局部效率为辅。

### 10.3 找出瓶颈 【R11-2-3】 *强制*
- 优化时应先找出限制效率的“瓶颈”，不要在无关紧要处优化。

### 10.4 先算法后代码 【R11-2-4】 *强制*
- 先优化数据结构和算法，再优化执行代码。

### 10.5 时空权衡 【R11-2-5】 *强制*
- 若时间效率和空间效率对立，需分析哪个更重要，做出适当折衷。

### 10.6 不追求紧凑代码 【R11-2-6】 *强制*
- 不要追求紧凑的代码，因为紧凑的代码并不能产生高效的机器码。

### 10.7 常见陷阱（建议）【S11-3-1 至 S11-3-14】 *建议*
- 当心视觉上不易分辨的操作符（如 `==` 误写为 `=`，丢失 `||`/`&&` 字符）。
- 变量（指针、数组）创建后及时初始化，防止未初始化使用。
- 当心变量初值、缺省值错误或精度不够。
- 当心数据类型转换错误，尽量使用显式转换，避免隐式转换。
- 当心变量上溢/下溢，数组下标越界。
- 当心忘记编写错误处理程序，或错误处理程序本身有误。
- 当心文件 I/O 错误。
- 避免编写技巧性很高的代码，优先考虑可读性。
- 不要设计面面俱到、过度灵活的数据结构，防止过度设计。
- 若原有代码质量好，尽量复用；但不要修补很差劲的代码，应重新编写。
- 尽量使用标准库函数，不要“发明”已存在的库函数。
- 尽量不要使用与具体硬件/软件环境关系密切的变量，增强可移植性。
- 将编译器选项设为最严格状态，以捕获更多潜在问题。
- 若可能，使用 PC-Lint、LogiScope 等工具进行代码审查。

---

## 附录：规范总结
- **强制性规则（Rules）**：必须严格遵守，违反将导致代码不合格。
- **建议（Suggestions）**：尽量采纳，有助于提高代码质量和可维护性。
- AI Agent 在生成代码时，应优先保证规则合规，同时应用合理建议，生成稳健、清晰、高效的 C++ 代码。
